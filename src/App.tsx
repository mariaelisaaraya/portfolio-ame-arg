import { BrowserRouter } from "react-router-dom";
import { RoutesConfig } from "./components/RoutesConfig";

function App() {
  return (
    <BrowserRouter>
      <RoutesConfig />
    </BrowserRouter>
  );
}

export default App;
